{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "prometheus-stack.serviceAccountName" . }}
  labels:
{{ include "prometheus-stack.labels" . | indent 4 }}
{{- end -}}
